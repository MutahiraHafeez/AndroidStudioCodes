<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example1.multimediaassignment01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MultimediaAssignment01">
        <activity android:name=".ActivityNasr"></activity>
        <activity android:name=".ActivityLahab" />
        <activity android:name=".ActivityIkhlas" />
        <activity android:name=".ActivityFalak" />
        <activity android:name=".SurahAnNas" />
        <activity android:name=".surah_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>